# Ignore everything in ssh.symlink except for the config file: helps avoid commiting keys
ssh.symlink/*
!ssh.symlink/config
!ssh.symlink/config.example
!ssh.symlink/config.local.example

# Ignore local gitconfig, if it exists
git/gitconfig.local.symlink

# Ignore mostly everything in vim.symlink as it will be generated by the installer
vim.symlink/*
!vim.symlink/install.sh
!vim.symlink/vimrc.symlink

# Ignore any certs, docker, and kerberos configuration so they aren't added into version control
certs/*
openssl/*
krb5/*
docker.symlink/*

.idea/
