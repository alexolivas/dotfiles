#!/usr/bin/env bash

# Secure copies a docker image from this computer to a remote host

IMAGE=$1
USER=$2
HOST=$3

rm -f /tmp/out.tar.gz
sudo docker save -o /tmp/out.tar $IMAGE && sudo gzip /tmp/out.tar
sudo scp /tmp/out.tar.gz $USER@$HOST:/home/$USER
ssh $USER@HOST sudo docker load -i out.tar.gz
